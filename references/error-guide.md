# 错误处理指引

| 错误场景 | 处理方式 |
|---------|---------|
| `convert.py` 非零退出 | 读终端输出：区分 Token 失效、URL 格式错误、IR 解析失败，对症处理 |
| Figma API 403/404 | 检查 token 有效期；确认 node-id 格式（`XX:YY`，不是 `XX-YY`） |
| `$DEST_DIR` 已有同名组件 | 询问用户：覆盖 / 加后缀重命名 / 跳过 |
| 综合评分 < 90 且修复 3 轮仍不达标 | 停止修复，向用户报告剩余偏差项及当前评分，由用户决策 |
